const fs = require('fs')
const path = require('path')
const { copyFolder, checkDirIsOk } = require('./sync-copy-files')

module.exports = {
  generateTheme(req, res) {
    console.info('88888888888888', req.body)
  }
}
